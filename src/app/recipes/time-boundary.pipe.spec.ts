import { TimeBoundaryPipe } from './time-boundary.pipe';

describe('TimeBoundaryPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeBoundaryPipe();
    expect(pipe).toBeTruthy();
  });
});
