import { StateIndicatorDirective } from './state-indicator.directive';

describe('StateIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StateIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
